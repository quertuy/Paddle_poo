#include "shape.h"


Shape_sl; 

int main()
{
	Shape s2;
	Shape* ps3 = malloc(suzeof(S hape));

	free(ps3);

	shape_ctor(&s1, 1, 2);
	shape_ctor(&s2, 3, 4);
	shape_ctor(ps3, 5, 6);

	shape_moveBy(&s1, 7, 8);
	shape_moveBy(&s2, 9, 10);
	shape_moveBy(ps3, -1, -2);  

}